@{
    ViewBag.Title = "Compress PDFs";
}


<div class="site-heading">
    @{Html.RenderPartial("_Nav"); }
</div>
<div class="content">
    <p class="lead">
        Upload multiple files that all need to be compressed. The download will contain those same PDF files, just compressed.
    </p>

    <form action="@Url.Action("HandleCompress", "PdfUtils")" method="post" enctype="multipart/form-data">
        <div class="form-group">
            <label>Select Files (up to 30Mb at a time)</label>
            <input type="file" multiple name="files" required />
        </div>
        <div class="form-group">
            <button class="btn btn-primary" type="submit">Compress</button>
        </div>
    </form>
</div>
